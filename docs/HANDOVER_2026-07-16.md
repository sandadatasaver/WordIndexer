# WordIndexer Handover — 2026-07-16

## Project

**Name:** WordIndexer  
**Owner:** Bishop David Sanda  
**License:** MIT  
**Repository:** `sandadatasaver/WordIndexer`

## Current release

**v0.4.0 — Optional Table-Cell Indexing**

The stable `main` branch contains the tested core, automatic INDEX-field feature, analysis reports, and optional table-cell indexing. The `develop` branch is for active Version 2 work.

## Validated capabilities

WordIndexer can now:

- Load a Microsoft Word `.docx` manuscript.
- Load a JSON dictionary.
- Search every occurrence of enabled terms.
- Search aliases and write canonical `index_as` terms.
- Resolve overlapping matches globally.
- Handle terms split across formatted runs.
- Detect an explicit TOC boundary when exposed by Word.
- Fall back to the first `Heading 1` beginning with `Chapter 1`.
- Traverse body paragraphs and top-level tables in document order when `--include-tables` is enabled.
- Insert Word-compatible XE fields after matched text.
- Preserve visible text and surrounding run formatting.
- Append an `Index` heading and Word `INDEX` field automatically.
- Save a new indexed DOCX without overwriting the source.
- Produce read-only console analysis reports.
- Export analysis reports as JSON.

## Validation results

The complete regression suite currently contains **37 passing tests**.

Validated documents:

- Sample DOCX.
- Torture DOCX.
- Bishop David Sanda's PowerShell manuscript.

Paragraph-only manuscript validation:

```text
Paragraphs        : 2867
Body starts       : 107
Ignored paragraphs: 107
Terms found       : 3
Terms missing     : 0
Total occurrences : 369
Overlaps resolved : 12
XE fields         : 369
```

Table-aware manuscript validation:

```text
Body starts       : 147
Terms found       : 3
Terms missing     : 0
Total occurrences : 1138
Overlaps resolved : 22
XE fields         : 1138
```

Microsoft Word successfully recognized the XE fields and generated page-numbered indexes. The automatic `Index` heading and INDEX field also worked after updating fields with `Ctrl+A`, then `F9`.

## Analysis and coverage reports

Example:

```powershell
python book_indexer.py analyze `
    input/sample.docx `
    dictionaries/technology/powershell.json `
    --json-output output/sample_analysis.json
```

The report includes:

- Body boundary.
- Ignored paragraphs.
- TOC detection method.
- Dictionary coverage.
- Missing terms.
- Total occurrences.
- Overlaps resolved.
- Per-term counts.

## Table-cell indexing

Table-cell indexing is opt-in:

```powershell
python book_indexer.py index `
    input/sample.docx `
    dictionaries/technology/powershell.json `
    output/sample_with_tables.docx `
    --include-tables
```

The default remains paragraph-only indexing.

## Current scope limitations

The engine does not yet search:

- Headers and footers.
- Footnotes and endnotes.
- Text boxes and other embedded Word stories.

These require separate traversal and regression tests.

## Important files

```text
book_indexer.py                 CLI
wordindexer/document.py         DOCX reader and ordered traversal
wordindexer/dictionary.py       JSON dictionaries
wordindexer/search.py           Search and occurrence collection
wordindexer/scanner.py          Exact run-level locations
wordindexer/matcher.py          Global overlap resolution
wordindexer/toc.py              TOC/body boundary detection
wordindexer/xmlwriter.py        Generic WordprocessingML writer
wordindexer/xe.py               XE field writer
wordindexer/index_field.py      Visible INDEX field writer
wordindexer/index.py            End-to-end indexing engine
wordindexer/reports.py          Dry-run analysis reports
```

## How to run

```powershell
python -m pytest -q
python book_indexer.py --version
python book_indexer.py inspect input/sample.docx
python book_indexer.py analyze input/sample.docx dictionaries/technology/powershell.json
python book_indexer.py index input/sample.docx dictionaries/technology/powershell.json output/sample_indexed.docx
```

The optional switches are:

- `--include-tables` — include table-cell paragraphs.
- `--no-index-field` — omit the automatic visible INDEX field.

## Repository hygiene

Do not commit:

- Private manuscripts.
- Generated DOCX files.
- Generated JSON/CSV/TXT reports.
- Temporary ZIP archives.
- Virtual environments or Python caches.

The source repository retains sample and torture documents only.

## Next milestones

1. Commit the v0.4.0 table-support release checkpoint.
2. Add headers and footers only if they are within the approved indexing scope.
3. Add footnotes, endnotes, and text boxes through separate tested traversals.
4. Add nested entries and cross references.
5. Add glossary and acronym services.
6. Build a GUI only after the core services are stable.

## Recovery point

If development needs to be paused, start from the latest pushed `main` release and inspect this handover plus `PROJECT.md`. Continue new work only on `develop`.
